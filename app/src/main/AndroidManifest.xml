<!-- AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lionico">

    <!-- Permissions (if needed in the future) -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Example permission -->

    <application
        android:name=".LionicoApplication" <!-- Hilt Application class -->
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lionico" <!-- App theme -->
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lionico">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add other activities or services here if needed -->

    </application>
</mani fest>
